# This file was autogenerated by Chef for host <%= node['fqdn'] %>
# Local changes will be overwritten
#

[cache]

<% @new_resource.subresources.select {|r| r.is_a?(Chef::Resource::CarbonCache) && r.action != :nothing }.each do |r| -%>
<%= r.to_ini -%>
<% end -%>

<% @new_resource.subresources.select {|r| r.is_a?(Chef::Resource::CarbonRelay) && r.action != :nothing }.each do |r| -%>
<%= r.to_ini -%>
<% end -%>

<% @new_resource.subresources.select {|r| r.is_a?(Chef::Resource::CarbonAggregator) && r.action != :nothing }.each do |r| -%>
<%= r.to_ini -%>
<% end -%>
